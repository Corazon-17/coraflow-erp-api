info:
  name: Create User
  type: http
  seq: 1

http:
  method: POST
  url: "{{apiUrl}}/users"
  body:
    type: json
    data: |-
      {
        "email": "desmond@gmail.com",
        "password": "00000000",
        "isInternal": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
